.DS_Store
*.log
node_modules/
.wrangler/
.dev.vars

# Secrets and local lead data (never commit)
.env.local
.env
data/
installers/google-credentials.json

# Scraped installer contact emails — local outreach use only, not for a public repo
installers/installer-emails.csv
installers/installer-profile-urls.csv
