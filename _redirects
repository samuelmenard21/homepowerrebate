# Cloudflare Pages redirects
# Format: /source  /destination  status
#
# Old flat city URLs -> canonical folder URLs.
# Kept so any pre-launch links or crawler-cached paths land on the right page.
/kelowna            /ca/bc/kelowna      301
/kelowna.html       /ca/bc/kelowna      301
